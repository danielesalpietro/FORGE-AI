#cloud-config
# FORGE-AI :: NoCloud vendor-data for {{ host.name }}
# Intentionally empty of configuration: everything the PoC needs lives
# in user-data. The file exists because some cloud-init versions log a
# 404 for a missing vendor-data, which is noise during a demo.
