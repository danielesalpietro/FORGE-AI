# FORGE-AI deployment report

| Field | Value |
|---|---|
| Deployment ID | `{{ report.deployment_id }}` |
| Environment | `{{ report.environment }}` |
| Git commit | `{{ report.git_commit }}` |
| Git branch | `{{ report.git_branch }}` |
| Started | `{{ report.started_at }}` |
| Finished | `{{ report.finished_at }}` |
| Duration | {{ report.duration_seconds }} s |
| Operator | `{{ report.operator }}` |
| Trigger | `{{ report.trigger }}` |
| Overall status | **{{ report.status | upper }}** |
{% if report.semaphore_task_url %}
| Semaphore task | [{{ report.semaphore_task_id }}]({{ report.semaphore_task_url }}) |
{% endif %}

## Hosts

{% for host in report.hosts %}
### {{ host.name }} — {{ host.status | upper }}

| Field | Value |
|---|---|
| Operating system | {{ host.os }} |
| Profile | `{{ host.profile }}` |
| IP address | `{{ host.ip_address }}` |
| MAC address | `{{ host.mac_address }}` |
| Firmware | {{ host.firmware | upper }} |
| Resources | {{ host.vcpu }} vCPU / {{ host.memory_mb }} MB / {{ host.disk_gb }} GB |
| Provisioning | {{ host.provisioning_result }} |
| Configuration | {{ host.configuration_result }} |
| Validation | {{ host.validation_result }} |
| Boot state | `{{ host.boot_state }}` |
| Install attempts | {{ host.install_attempts }} |
| Duration | {{ host.duration_seconds }} s |

{% if host.failed_tasks | length > 0 %}
**Failed tasks ({{ host.failed_tasks | length }}):**

{% for task in host.failed_tasks %}
- `{{ task.task }}` — {{ task.msg | default('no message') }}
{% endfor %}
{% else %}
No failed tasks.
{% endif %}

{% if host.facts %}
<details><summary>Collected facts</summary>

| Fact | Value |
|---|---|
{% for key, value in host.facts.items() %}
| {{ key }} | `{{ value }}` |
{% endfor %}

</details>
{% endif %}

{% endfor %}

## Source media

| Artifact | Path | SHA-256 |
|---|---|---|
{% for item in report.media %}
| {{ item.name }} | `{{ item.path }}` | `{{ item.sha256 | default('not recorded') }}` |
{% endfor %}

## Component versions

| Component | Version |
|---|---|
{% for name, version in report.versions.items() %}
| {{ name }} | `{{ version }}` |
{% endfor %}

## Logs

{% for entry in report.logs %}
- **{{ entry.component }}** — `{{ entry.location }}`{% if entry.hint %} ({{ entry.hint }}){% endif %}

{% endfor %}

---

_Generated by FORGE-AI on {{ report.finished_at }}. Machine-readable
sibling: `{{ report.json_path }}`._
